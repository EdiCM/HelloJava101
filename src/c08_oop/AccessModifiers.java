package c08_oop;

public class AccessModifiers {
    public static void main(String[] args) {

        /*
         Encapsulamiento - (modificadores de acceso)
         Encapsular es proteger los datos de una clase para controlar el acceso desde afuera
         ~ Public: se puede acceder desde cualquier parte del programa
         ~ Private: solo se accede desde la clase donde lo declare
         ~ Protected: lo mismo que default + las subclases o carpetas con acceso
         ~ Default: solo se accede dentro de la carpeta donde lo declare

         Por buena practica las clases siempre son publicas

         Getters y Setters
         ~ Metodos o funciones para permitir acceso a la variable en cuestion que hemos limitado
         Set = para modificar o aplicar una logica de validacion al estado interno de un objeto
         DESPUES de ser construido, si no quiero hacer esto pues lo hago final. SIEMPRE debe ser
         void y SIEMPRE debe recibir un parametro de entrada.

         Get = accede a la clase para devolvernos el valor de un atributo que queramos
        */

    }
}
